const express = require("express");
const userRouter = express.Router()

// userRouter.get("/", articleController.getArticles);
// userRouter.post("/", articleController.createArticle)

module.exports = userRouter;
